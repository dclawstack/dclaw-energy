apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dclaw-energy.serviceAccountName" . }}
  labels:
    {{- include "dclaw-energy.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
