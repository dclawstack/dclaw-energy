apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-energy.fullname" . }}
  labels:
    {{- include "dclaw-energy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-energy.selectorLabels" . | nindent 4 }}
